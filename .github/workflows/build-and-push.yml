name: Continuous Integration-Continuous Deployment

on:
  push:
    branches:
      - master

jobs:
  docker-build:
    runs-on: ubuntu-latest
    name: Docker Image Build
    steps:
    - uses: actions/checkout@v2
    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ env.DOCKERHUB__USERNAME }}
        password: ${{ env.DOCKERHUB__TOKEN }}
    - name: Build Docker Image
      uses: docker/build-push-action@v2
      with:
        context: .
        file: ./ci/docker/Dockerfile
        push: true
        tags: simonovm/tonometer_statistics:latest

  deploy:
    runs-on: ubuntu-latest
    name: Continuous Deployment
    needs: [docker-build]
    steps:
      - name: Deploy using ssh
        uses: appleboy/ssh-action@master
        with:
          host: ${{ env.SSH__HOST }}
          username: ${{ env.SSH__USERNAME }}
          key: ${{ env.SSH__KEY }}
          port: ${{ env.SSH__PORT }}
          script: |
            echo "${{ env.ENV_FILE }}" > /opt/tonometer_statistics/.env;
            cd /opt/tonometer_statistics/;docker stop tonometer_statistics;docker rm tonometer_statistics;docker pull simonovm/tonometer_statistics:latest;docker-compose up --build -d
